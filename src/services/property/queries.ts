import { gql } from "@apollo/client";

// Placeholder queries - these will be replaced when property service is ready
// Using the actual queries in the Apollo client mock for now

export const GET_PROPERTIES = gql`
  query GetProperties {
    __typename
  }
`;

export const GET_PROPERTY_BY_ID = gql`
  query GetPropertyById {
    __typename
  }
`;
